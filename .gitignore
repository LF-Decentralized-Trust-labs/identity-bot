# Go
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/

# Flutter
identity_agent_ui/build/
identity_agent_ui/.dart_tool/
identity_agent_ui/.packages
identity_agent_ui/.pub-cache/
identity_agent_ui/.pub/
*.dart.js
*.dart.js.deps
*.dart.js.map

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code (local permissions, worktrees, and runtime lock files — machine-specific, not for VCS)
.claude/settings.local.json
.claude/worktrees/
.claude/*.lock

# macOS
.DS_Store

# Environment
.env
.env*.local

# Runtime data (SQLite, PID files, endpoint state)
data/
data-*/

# Python
__pycache__/
tests/keri_test_vector.json

# Replit
.cache/
.config/
.local/
attached_assets/

# MCP config (personal dev tooling)
.mcp.json

# Claude Code project instructions (local-only, not for public repo)
CLAUDE.md

# Local personal tooling (markdown-to-docx converter, etc.)
md_to_docx.py

# Runtime agent state (AGENT_DATA_DIR) — never commit
data/
